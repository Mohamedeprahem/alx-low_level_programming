#include <stdio.h>
#include <stdlib.h>
#include "dog.h"

/**
 * print_dog - a function that prints a struct dog
 *@d: d dog
 *
 * Return: void
 */
void print_dog(struct dog *d)
{
if (d == NULL)
return (0)
if d.name == NULL
printf("d.name %s", nil);

if d.owner = NULL
printf("d.owner %s", nil);
printf("age %f", d.age);
}

