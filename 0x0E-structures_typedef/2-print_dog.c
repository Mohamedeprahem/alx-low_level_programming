#include <stdio.h>
#include <stdlib.h>
#include "dog.h"

/**
 * print_dog - a function that prints a struct dog
 *@d: d dog
 *
 * Return: void
 */
void print_dog(struct dog *d)
{
if (d == NULL)
return (0)
if d.name == NULL
d.name = "(nil)"
printf("d.name %s", d.name);

if d.owner = NULL
d.owner = "(nil)"
printf("d.owner %s", (nil));
printf("age %f", d.age);
}

