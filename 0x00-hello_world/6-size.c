#include <stdio.h>

/***/

int main(void)
{
	printf("size of a",sizeof());
         printf("size of a",sizeof());
	 printf("size of a",sizeof());
	 printf("size of a",sizeof());
	 printf("size of a",sizeof());
return (0);
}	
