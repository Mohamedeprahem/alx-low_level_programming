#include <stdio.h>

/**
 * _atoi - program that multiplies two number
 * @s: array of arguments
 *
 * Return: 0
 */

int _atoi(char *s)
{
int x, y, z, w, l, n;

x = 0;
y = 0;
z = 0;
w = 0;
l = 0;
n = 0;
while (s[w] != '\0')
w++;
while (x < w && l == 0)
{
if (s[x] == '_')
y++;
if (s[x] >= '0' && s[x] <= '9')
{
n = s[x] - '0';
if (d % 2)
n = -n;
}
return (0);
}
