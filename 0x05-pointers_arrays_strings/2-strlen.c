#include "main.h"
#include <stdio.h>

/**
 * _strlen -  function that returns the length of a string
 *
 *@s : char 
 *
 *
 * Retrun : Always 0
\*

 int _strlen(char *s)
{

int y = 0
while (*s!= '\0')
y++;
s++;
return (0);
}
