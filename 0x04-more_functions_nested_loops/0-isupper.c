#include "main.h"
#include <stdio.h>

/**
 *_isupper - checks for uppercase character.
 *
 * @c: input for alphabet
 * Return: 1 if uppercase, 0 if lower
 */
int _isupper(int c);
{
if (c >= 'a' && c <= 'z')	
return (1);
else
return (0);
}
