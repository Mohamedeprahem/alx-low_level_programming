#include "main.h"

/**
 * string_toupper - changes all lowercase letters of a string to uppercase
 *
 * @r: value
 * Return: r
 */

char *cap_string(char *n)
{
char *ptr = n;
int x = 1;
while (*n)
for (s = 0; r[s] != '\0'; s++)
{
if (r[s] >= 97 && r[s] <= 122)
{
r[s] = r[s] - 32;
}
}
return (r);
}
